<!DOCTYPE html>
<html class="{{ html_classes }}">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
	<meta http-equiv="X-UA-Compatible" content="ie=edge">
	<meta http-equiv="Content-Language" content="en" />
	<meta name="msapplication-TileColor" content="#2d89ef">
	<meta name="theme-color" content="#4188c9">
	<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent"/>
	<meta name="apple-mobile-web-app-capable" content="yes">
	<meta name="mobile-web-app-capable" content="yes">
	<meta name="HandheldFriendly" content="True">
	<meta name="MobileOptimized" content="320">
	<link rel="icon" href="./favicon.ico" type="image/x-icon"/>
	<link rel="shortcut icon" type="image/x-icon" href="./favicon.ico" />

	<title>TFG Dashboard</title>

	<link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:300,300i,400,400i,500,500i,600,600i,700,700i&amp;subset=latin-ext">
	<link rel="stylesheet" href="{{ twg_func( 'assets_url') }}fonts/font-awesome.min.css">

	<script src="{{ twg_func( 'assets_url') }}/js/require.min.js"></script>
	<script>
		requirejs.config({
			baseUrl: '{{ twg_func( "base_url") }}.'
		});
	</script>

	<link href="{{ twg_func( 'assets_url') }}/css/dashboard.css" rel="stylesheet" />
	<link href="{{ twg_func( 'assets_url') }}/css/custom.css" rel="stylesheet" />
	<script src="{{ twg_func( 'assets_url') }}/js/dashboard.js"></script>

	{% block head %}{% endblock %}
</head>
<body class="preload {% if body_tags %} {{ body_tags }}{% endif %}">

	<div class="page">
		<div class="page-{% if page_simple %}simple{% else %}main{% endif %}">

			<!-- Header -->
			{% if not no_header %}
				{% include [ theme_path, '/layout/header.twig' ] | join %}
			{% endif %}
			{% if not no_breadcrumb %}
            	{% include [ theme_path, '/layout/breadcrumb.twig' ] | join %}
            {% endif %}
			<!-- End Header -->

			<!-- Main -->
			{% block content %}{% endblock %}
			<!-- End Main -->
		</div>

		<!-- Footer -->
		{% if not no_footer %}
			{% include [ theme_path, '/layout/footer.twig' ] | join %}
		{% endif %}
		<!-- End Footer -->
	</div>

	<!-- Scripts bottom -->
	{% block ol_scripts_bottom %}{% endblock %}
	<!-- # Scripts bottom -->
</body>
</html>
